#include "holberton.h"

/**
 * _islower - checks for lowercase
 *
 * @c: int
 *
 * Return: void
 */

int _abs(int n)
{

  if (n > 0)
    return (n);
  else if (n < 0)
    return (-n);
  else
    return (0);
}
